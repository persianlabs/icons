{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "assets/**/*.svg"],
      "outputs": ["dist/**", "icons.json", "icons-mono.json", "src/generated/**"]
    }
  }
}
